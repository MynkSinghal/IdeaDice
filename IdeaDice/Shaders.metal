#include <metal_stdlib>
using namespace metal;

// This is an empty Metal shader file
// It's only purpose is to ensure a default.metallib is created during build
// This allows the app to find a Metal library at runtime 